# URL бэкенда (если не используете proxy в vite.config.ts).
# При npm run dev с прокси можно не задавать — запросы пойдут на тот же хост и проксируются на бэкенд.
# Пример для продакшена или отдельного запуска фронта:
# VITE_API_BASE_URL=http://localhost:8000
